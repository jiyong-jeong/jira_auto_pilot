{
  "name": "jira-claude-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Jira -> Claude 루프 자동화 제어/모니터링 대시보드 (React + Express)",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch lib.js server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@babel/preset-react": "^7.29.7",
    "@babel/standalone": "^7.29.7",
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
