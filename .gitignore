# 비밀 / 런타임 설정
*.env
dashboard/config.json
dashboard/credentials.json
dashboard/projects.json
dashboard/project-credentials.json

# clone 된 작업 디렉토리 (대상 repo들이 모이는 곳)
repos/
# (구버전 호환) 과거 평면 clone 디렉토리
kyb-api/
kyb-api-*/
*-[A-Z]*-[0-9]*/

# 로그
*.log

# 처리 이력 (런타임 생성)
history.jsonl

# 루프 pidfile (런타임 생성)
loop-*.pid

# claude 카드별 실행 로그 (런타임 생성)
claude-logs/

# 의존성
node_modules/

# OS / 로컬
.DS_Store
.claude/
